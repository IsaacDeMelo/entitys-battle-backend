<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Goofymon - Login</title>
    <link href="https://fonts.googleapis.com/css2?family=Press+Start+2P&family=Roboto:wght@400;700&display=swap" rel="stylesheet">
    <style>
        body { background: #0f0c29; color: white; font-family: 'Roboto', sans-serif; display: flex; flex-direction: column; align-items: center; justify-content: center; height: 100vh; margin: 0; }
        .card { background: #1a1a2e; padding: 30px; border-radius: 20px; text-align: center; border: 2px solid #3498db; box-shadow: 0 0 30px rgba(52, 152, 219, 0.4); width: 90%; max-width: 400px; }
        h1 { font-family: 'Press Start 2P'; color: #f1c40f; margin-bottom: 20px; font-size: 1.5rem; }
        input { padding: 15px; width: 100%; margin-bottom: 20px; background: #222; border: 1px solid #444; color: white; border-radius: 10px; font-size: 1.2rem; text-align: center; box-sizing: border-box; }
        
        .char-select { display: flex; justify-content: center; gap: 10px; margin-bottom: 20px; flex-wrap: wrap; }
        .char-wrapper { position: relative; cursor: pointer; }
        .char-wrapper input { display: none; }
        
        .char-view { 
            width: 60px; height: 60px; 
            border: 2px solid #444; border-radius: 10px; 
            display: block; 
            background-size: 400% auto; 
            background-position: 0 0;
            image-rendering: pixelated;
            transition: 0.2s;
        }
        
        input:checked + .char-view { border-color: #f1c40f; transform: scale(1.1); box-shadow: 0 0 10px #f1c40f; }
        button { padding: 15px 30px; background: #2ecc71; color: white; border: none; border-radius: 50px; font-size: 1.2rem; cursor: pointer; font-weight: bold; width: 100%; transition: 0.2s; }
        button:hover { transform: scale(1.05); background: #27ae60; }
    </style>
</head>
<body>
    <div class="card">
        <h1>GOOFYMON</h1>
        <form action="/room" method="POST">
            <p style="margin-bottom:10px; color:#aaa;">Seu Nome</p>
            <input type="text" name="playerName" required maxlength="12">

            <p style="margin-bottom:10px; color:#aaa;">Escolha seu Personagem</p>
            <div class="char-select">
                <!-- GERA BOTÕES AUTOMATICAMENTE BASEADO NA QUANTIDADE -->
                <% for(let i = 1; i <= skinCount; i++) { %>
                    <label class="char-wrapper">
                        <input type="radio" name="charId" value="char<%= i %>" <%= i===1 ? 'checked' : '' %>>
                        <!-- Define imagem inline, assim funciona com qualquer número -->
                        <span class="char-view" style="background-image: url('/uploads/char<%= i %>.png');"></span>
                    </label>
                <% } %>
            </div>

            <button type="submit">ENTRAR</button>
        </form>
    </div>
</body>
</html>