<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Criar</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Roboto', sans-serif; background: #121212; color: white; padding: 20px; margin: 0; padding-bottom: 50px; }
        h1 { text-align: center; color: #3498db; }
        label { display: block; margin-top: 15px; color: #aaa; margin-bottom: 5px; }
        input, select { width: 100%; padding: 15px; background: #1e1e2e; border: 1px solid #333; color: white; border-radius: 10px; font-size: 16px; box-sizing: border-box; }
        .row { display: flex; gap: 10px; } .col { flex: 1; }
        .file-box { border: 2px dashed #444; padding: 30px; text-align: center; border-radius: 10px; margin-top: 5px; background: #1a1a20; }
        .move-item { background: #1e1e2e; padding: 15px; margin-bottom: 10px; border-radius: 10px; display: flex; align-items: center; border: 1px solid #333; }
        .move-item input { width: 25px; height: 25px; margin-right: 15px; }
        .btn-save { width: 100%; padding: 20px; background: #2ecc71; color: white; border: none; border-radius: 50px; font-size: 1.2rem; font-weight: bold; margin-top: 30px; box-shadow: 0 4px 15px rgba(46, 204, 113, 0.4); }
    </style>
</head>
<body>
    <h1>Novo Lutador</h1>
    <form action="/create" method="POST" enctype="multipart/form-data">
        <label>Nome</label><input type="text" name="name" required placeholder="Ex: Guerreiro X">
        <label>Tipo</label><select name="type"><% for (let t in types) { %><option value="<%= types[t] %>"><%= types[t].toUpperCase() %></option><% } %></select>
        <label>Imagem (GIF/PNG)</label>
        <div class="file-box" onclick="document.getElementById('f').click()">
            <span style="font-size: 2rem;">📸</span><br><span id="fname" style="color:#3498db">Toque para escolher</span>
            <input type="file" id="f" name="sprite" accept="image/*" style="display:none" onchange="document.getElementById('fname').innerText = this.files[0].name">
        </div>
        <div class="row"><div class="col"><label>HP</label><input type="number" name="hp" value="100"></div><div class="col"><label>Energia</label><input type="number" name="energy" value="50"></div></div>
        <div class="row"><div class="col"><label>Atk</label><input type="number" name="attack" value="10"></div><div class="col"><label>Def</label><input type="number" name="defense" value="5"></div><div class="col"><label>Spd</label><input type="number" name="speed" value="10"></div></div>
        <label>Golpes (Escolha 4)</label>
        <% for (let k in moves) { %><div class="move-item"><input type="checkbox" name="selectedMoves" value="<%= k %>"><div><div style="font-weight:bold"><%= moves[k].icon %> <%= moves[k].name %></div><div style="font-size:0.8rem; color:#aaa"><%= moves[k].cost %> Energy</div></div></div><% } %>
        <button type="submit" class="btn-save">SALVAR</button>
        <a href="/" style="display:block; text-align:center; margin-top:20px; color:#666; text-decoration:none;">Cancelar</a>
    </form>
</body>
</html>